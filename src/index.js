import React, { useState, createContext } from "react";
import ReactDOM from "react-dom";

const toggleForm = createContext()

import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));
